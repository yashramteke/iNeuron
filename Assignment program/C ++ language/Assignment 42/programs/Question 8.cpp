/*Q8. Insert a new element into the queue container, the new element is added to the end
of the queue*/

#include <iostream>
#include <queue>
using namespace std;

int main()
{
    queue <int> v;

    v.push(10);
    v.push(999);
}
